const snacks: string[] = [
    "Chips",
    "Cookies",
    "Cheetoes",
    "Skittles",
    "Chocolate"
  ];

console.log(snacks);

export default function logMessage(message: string): void {
    console.log(snacks); 
    //trying to change file
}
